describe('Login Effects', () => {
  it('should handle a login start action', () => pending());
  it('should handle a login action success', () => pending());
  it('should handle a login action fail', () => pending());
  it('should handle a logout action', () => pending());
  it('should handle a remember me action', () => pending());
  it('should handle a forget me action', () => pending());
});
