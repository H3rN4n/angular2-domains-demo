describe('Login Effects', () => {
  it('should handle a login start action', () => pending());
  it('should handle a login complete action', () => { pending(); });
  it('should handle a login fail action', () => pending());
  it('should handle a logout start action', () => pending());
  it('should handle open login modal action', () => pending());
  it('should handle close login modal action', () => pending());
  it('should handle a remember me action', () => pending());
  it('should handle a forget me action', () => pending());
});
